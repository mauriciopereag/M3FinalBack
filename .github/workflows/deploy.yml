name: Deploy Backend

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18.x'
      - name: Install dependencies
        run: npm install
        working-directory: ./backend
      - name: Deploy (placeholder)
        run: echo "Despliegue exitoso (personaliza este paso según tu entorno)"
        working-directory: ./backend 